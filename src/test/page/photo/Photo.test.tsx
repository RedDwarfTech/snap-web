import GenPhoto from "@/page/photo/gen/GenPhoto";
import { render, screen } from '@testing-library/react';

test('Test image download', () => {
  render(<GenPhoto />);
  const a = 1;
  const base64 = 'abcdefghijklmnopqrstuvwxy';



});